query room($name: String!) {
  room(name: $name) {
    ...RoomFields
  }
}

query rooms($name: String!) {
  rooms(name: $name) {
    ...RoomFields
  }
}

fragment RoomFields on Room {
  id
  name
  title
  state
  avatar
  chatId
}